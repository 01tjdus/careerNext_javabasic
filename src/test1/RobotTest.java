/*
package test1;

public class RobotTest {
    public static void main(String[] args) {
        // 1. 배터리 만들고 200 충전
        Battery b = new Battery();
        b.charge(200);

        // 2. 로봇 만들고 배터리 장착
        Robot r = new Robot();
        r.name = "JENNIE";
        r.robotEnergy = b;   // 숫자 아님, 위에서 만든 배터리

        // 3. 이동 / 뛰기
        r.move(2);
        r.run(5);

        // 4. 남은 잔량 출력
        System.out.println("남은 배터리 : " + b);
    }
}
 */